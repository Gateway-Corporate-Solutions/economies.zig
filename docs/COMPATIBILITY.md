# Compiler compatibility

| Package version | Zig version | Status |
| --- | --- | --- |
| `0.1.0-dev.1` | `0.16.0` | Development release candidate; exact compiler tested locally |

Future main-branch compiler migrations will use a separate branch until a compatible immutable release exists. A tag supporting Zig 0.16.0 remains immutable after publication.
